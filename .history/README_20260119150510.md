# 🎬 Virex

## Установка

### Windows
```cmd
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### Linux
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Запуск

1. Получи токен у @BotFather
2. Открой `config.py`, замени `YOUR_BOT_TOKEN_HERE`
3. Запусти:

```bash
python bot.py
```

## FFmpeg

Windows: [ffmpeg.org/download](https://ffmpeg.org/download.html) → добавь в PATH

Linux: `sudo apt install ffmpeg`
